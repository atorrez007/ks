export type SocialType = {
  id: number;
  name: string;
  url: string;
  target: string;
  style: object;
};

export type ProjectType = {
  id: number;
  title: string;
  imgUrl: string;
  description: string;
  tags: string[];
  link: string;
};
